"use strict";
let fruits = ["orange", "kiwi", "apple"];
//Create a new array named citrusFruits that contains the first
// two elements of the fruits array using the method.
let fruitArrayElement = fruits.slice(0, 2);
let citriusFruits = fruitArrayElement;
console.log(citriusFruits);
